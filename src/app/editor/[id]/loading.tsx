import { ScreenLoader } from "@/components/ui/screen-loader";

export default function EditorLoading() {
  return (
    <ScreenLoader
      text="Loading Page Builder..."
      subtitle="Preparing visual editor, blocks, and media components"
    />
  );
}
